{
  "name": "ai-skills",
  "private": true,
  "description": "Command surface for this repo's checks. No runtime dependencies — the scripts are bash + python3.",
  "scripts": {
    "validate": "./scripts/validate.sh",
    "test:release-steps": "./scripts/test-release-steps.sh",
    "test": "npm run validate && npm run test:release-steps"
  }
}
